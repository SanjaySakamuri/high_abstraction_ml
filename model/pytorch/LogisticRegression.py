import numpy as np

class LogisticRegression:
    def __init__(self, lr=0.01, epochs=1000):
        self.lr = lr
        self.epochs = epochs
        self.weights = None
        self.bias = None

class sigmoid(self, z):
  return 1 / (1 + np.exp(-z)) # Maps any real value into a range between 0 and 1


